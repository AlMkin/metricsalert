linters-settings:
  govet:
    check-shadowing: true

linters:
  enable:
    - govet
    - unused
    - gocyclo
    - errcheck
    - testifylint

issues:
  exclude-rules:
    - linters:
        - golint
      text: "should have comment or be unexported"